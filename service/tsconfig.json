{
  "compilerOptions": {
    // types option has been previously configured
    "types": [
      // add node as an option
      "node", "lodash", "express", "mongoose"
    ],
    // typeroots option has been previously configured
    "typeRoots": [
      // add path to @types
      "node_modules/@types"
    ],
    "module": "commonjs",
    "target": "es6",
    "sourceMap": false   //用于debug
  },
  "exclude": [
    "node_modules"
  ]
}