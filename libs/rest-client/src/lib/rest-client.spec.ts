import { RestClient } from './rest-client';
import { HttpClient } from '@angular/common/http';

describe('RestClient', () => {
  it('should create an instance', () => {
    // expect(new RestClient(HttpClient)).toBeTruthy();
  });
});
